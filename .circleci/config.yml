version: 2
jobs:
   build:
     machine: true
     post:
       - sudo -H pip install --upgrade awscli
     steps:
       - checkout
       - run: |
            pwd
            echo
            ls -al
            echo
            ls -al ~/
            echo
            ls -al /
            echo
            aws --version
            echo
            aws ecr describe-repositories --registry-id 307921801440
