version: 2
jobs:
   build:
     machine: true
     post:
       - sudo pip install --upgrade awscli
     steps:
       - checkout
       - run: |
            pwd
            echo
            ls -al
            echo
            ls -al ~/
            echo
            ls -al /
            echo
            aws --version
